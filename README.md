It is a rest API. We provide functionalities to add, update, delete and read friends. The DB name will be frnd and collection name will be friends. 

This is programed using Node JS and Express JS and Mongo DB. I have used MEAN stack (excluding Angular JS).
